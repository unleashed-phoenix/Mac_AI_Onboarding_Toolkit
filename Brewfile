# Mac_AI_Onboarding_Toolkit Brewfile
# Reproduce this machine's toolchain on a fresh Mac:
#   brew bundle --file=Brewfile
# Snapshot current machine into a Brewfile:
#   brew bundle dump --force --file=Brewfile

# ---- Core toolchain (already installed on this machine) ----
brew "git"
brew "uv"
brew "gh"

# ---- API testing (Bruno — Postman replacement, never Postman) ----
cask "bruno"

# ---- Local models ----
cask "ollama"
# cask "lm-studio"   # optional GUI for local models — enable in Phase 9

# ---- Editors (already installed) ----
# cask "visual-studio-code"
# cask "cursor"

# ---- Productivity (already installed) ----
# cask "raycast"
# cask "rectangle"
# cask "iterm2"

# ---- Pre-commit (installed via uv tool, see .pre-commit-config.yaml) ----
